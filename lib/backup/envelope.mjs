// Marker for encrypted backup files. Plain JavaScript so the browser, the server
// and the GitHub backup workflow can all import it.
export const ENVELOPE_FORMAT = "family-portfolio-encrypted-backup"
